import React, { Component } from "react";

class Buttons extends Component {
  render() {
    return (
      <div>
        <button>증가</button>
        <button>감소</button>
      </div>
    );
  }
}

export default Buttons;
